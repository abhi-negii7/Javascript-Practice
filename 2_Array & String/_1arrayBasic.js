// C++ : Vectors, Java : ArrayList
arr = [1, 2, 3, 4];

//size of array
n = arr.length;

//Access the array using index
console.log(arr[0]);

//insert an element at last in array
arr.push(10);
console.log(arr);

//insert an element at front in array
arr.unshift(20);
console.log(arr);

//remove an element at last in array
// arr.pop();

//remove an element at front in array
arr.shift();
